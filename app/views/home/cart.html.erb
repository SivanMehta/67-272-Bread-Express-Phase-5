<p>
    Wow that's some nice stuff you're ordering there!
</p>

<% if session[:cart].nil? %>
    Cart is Nil
<% elsif session[:cart].empty? %>
    Cart is Empty
<% else %>
    <%= session[:cart] %>
<% end %>