<br />
<% if !current_user.nil? and current_user.role? :admin %>
    <%= link_to ("<i class='mdi-content-add left'></i> New Customer").html_safe, new_customer_path, :encode => "javascript", :class => "btn waves-light green lighten-1" %>
<% end %>

<%= render partial: "partials/index_structure", locals: {model_name: "customer", 
                                                         primary: "active_customers",
                                                         secondary: nil,
                                                         sidebar: "inactive_customers"} %>