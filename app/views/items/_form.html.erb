<%= simple_nested_form_for(@item) do |item_form| %>
<% if @item.errors.any? %>
    <ul class = "collection with-header">
        <li class = "collection-header">
            <h4>
                <i class = "mdi-alert-error"></i>
                <%= pluralize(@item.errors.count, "error") %> prohibited this item from being saved:
            </h4>
        </li>
        <% @item.errors.full_messages.each do |msg| %>
            <li class = "collection-item"><i class = "mdi-communication-dnd-on"></i> <%= msg %></li>
        <% end %>
        </ul>
  <% end %>

    <%= render "item_fields", :f => item_form %>

    <%= item_form.simple_fields_for :item_prices do |item_price_form| %>
        <%= render "item_price_fields", :f => item_price_form %>
    <% end %>

    <div class = "actions">
        <button class="btn green" type="submit" name="action">
          <i class='mdi-content-add left'></i> Create Item
      </button>
    </div>
<% end %>